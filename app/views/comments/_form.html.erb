<%= form_for([@article, @article.comments.build]) do |f| %>
  <div class="well">
    <%= f.label :Comment %><br>
    <%= f.text_area :body %>
    
    <%= f.submit "Create Comment",  class: "btn btn-lg btn-success" %>
  </div>
<% end %>